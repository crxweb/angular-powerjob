import { EmploiStorePipe } from './emploi-store.pipe';

describe('EmploiStorePipe', () => {
  it('create an instance', () => {
    const pipe = new EmploiStorePipe();
    expect(pipe).toBeTruthy();
  });
});
