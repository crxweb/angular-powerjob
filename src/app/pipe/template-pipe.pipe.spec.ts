import { TemplatePipePipe } from './template-pipe.pipe';

describe('TemplatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TemplatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
